# azure-devops-workitems-migration
Python stdlib-only scripts to migrate Azure DevOps work items (parents, children, links, comments, attachments) from a source project to a target project using Azure DevOps REST APIs.
